<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.samplelistview"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="10" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.samplelistview.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="StrAdapterActivity"></activity>
        <activity android:name="StrToStringActivity"></activity>
        <activity android:name="BookArgActivity"></activity>
        <activity android:name="BookBaseInnerActivity"></activity>
        <activity android:name="BookBaseExtActivity"></activity>
        <activity android:name="BookArrInnerActivity"></activity>
        <activity android:name="BookArrExtActivity"></activity>
    </application>

</manifest>